# Yandex SpeechKit & GPT Telegram Bot

Бот для автоматического распознавания голосовых сообщений, аудиофайлов и архивов.
Использует **Yandex SpeechKit** (STT) для транскрибации и **YandexGPT** для анализа текста (саммари, тональность).

## 🚀 Возможности
- 🎤 **Голосовые и Аудио**: Поддержка любых форматов.
- 📦 **Архивы ZIP**: Можно отправить архив с 50+ файлами, бот сам распакует и обработает каждый.
- ⏳ **Очередь задач**: Файлы обрабатываются в фоне, не блокируя бота.
- 📢 **Отчеты в группу**: Результаты приходят в специальный чат для менеджеров.
- 📊 **Аналитика**:
    - "Литературная" обработка текста.
    - Саммари (краткое содержание).
    - Тональность (Позитив/Негатив) + Хештеги.
- 🔘 **Интерактив**: Кнопка "Взять в работу" для командной работы.

## 🛠 Установка и Запуск

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/bassixs/vodokanal.git
   cd vodokanal
   ```

2. **Создайте окружение и установите зависимости**:
   ```bash
   python -m venv .venv
   .venv\Scripts\activate  # Windows
   # source .venv/bin/activate # Linux/Mac
   pip install -r requirements.txt
   ```

3. **Настройка .env**:
   Создайте файл `.env` на основе примера.
   
   **Как получить TARGET_CHAT_ID:**
   1. Добавьте бота в нужную группу (можно закрытую).
   2. Напишите там команду `/id`.
   3. Бот пришлет ID (например `-10012345678`). Вставьте его в `.env`.

4. **Запуск**:
   ```bash
   python -m bot.main
   ```

## 🐳 Развертывание (Ubuntu Server)
См. файл [DEPLOY.md](DEPLOY.md) для подробной инструкции по запуску на сервере (systemd).

## 📂 Структура проекта
- `bot/main.py`: Точка входа.
- `bot/worker.py`: Фоновый процесс обработки очереди.
- `bot/handlers.py`: Обработка сообщений Telegram.
- `bot/services/`: Интеграции с Yandex Cloud и SQLite.
